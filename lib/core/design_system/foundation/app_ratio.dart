/// 화면 요소의 가로:세로 비율 토큰.
abstract final class AppRatio {
  const AppRatio._();

  /// 따라찍기 사진 프레임. (가로:세로 = 3:4)
  ///
  /// 같은 사진이 촬영 확인 → 모임 헤더 → 갤러리 카드 → 크게 보기로 이어지므로,
  /// 단계마다 다르게 잘리지 않도록 이 비율 하나로 맞춘다. 높이를 고정하지 않고
  /// 비율로 두어 화면 폭이 달라져도 모양이 유지된다.
  static const double photo = 3 / 4;
}
